import {useMutation} from '@apollo/client';

function useChatService() {
  cosnt[createChat] = useMutation();
  const createChatWithUser = () => {};

  return {createChatWithUser};
}

export default useChatService;
